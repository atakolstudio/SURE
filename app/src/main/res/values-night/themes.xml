<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SURE.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/sure_background_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_sure_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.SURE</item>
    </style>

    <style name="Theme.SURE" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">#10131A</item>
    </style>
</resources>
